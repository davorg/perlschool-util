#!/usr/bin/env perl
use v5.38;
use warnings;

use FindBin qw($RealBin);
use lib "$RealBin/../lib";
use PerlSchool::App::LintMS;

my $app = PerlSchool::App::LintMS->new(
  files => \@ARGV,
);

exit $app->run_app();

