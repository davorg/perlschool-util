#!/usr/bin/env perl
use v5.38;
use warnings;

use FindBin qw($RealBin);
use lib "$RealBin/../lib";
use PerlSchool::App::LintMS;

@ARGV or die "Usage: $0 file1.md [file2.md ...]\n";

my $app = PerlSchool::App::LintMS->new(
  files => \@ARGV,
);

exit $app->run_app();

